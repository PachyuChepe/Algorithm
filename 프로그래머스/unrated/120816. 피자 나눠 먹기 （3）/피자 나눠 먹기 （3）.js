function solution(slice, n) {
    // console.log("뭐임", slice)
    // console.log("뭐임?", n)
    // n => 졷간
    // slice => 휫짜
    console.log(Math.ceil(n/slice))
    var answer = Math.ceil(n/slice)
    return answer;
}